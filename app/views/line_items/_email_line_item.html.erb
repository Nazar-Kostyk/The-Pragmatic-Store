<% if email_line_item == @current_item %>
<tr class="line-item-highlight">
<% else %>
<tr>
<% end %>
  <td class="quantity"><%= email_line_item.quantity %></td>
  <td><%= email_line_item.product.title %></td>
  <td class="price"><%= number_to_currency(email_line_item.total_price) %></td>
</tr>